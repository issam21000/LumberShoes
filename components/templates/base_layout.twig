<!doctype html>
<html lang="fr">

	<head>

		<link rel="stylesheet" type="text/css" href="/Assets/css/bootstrap.min.css">
		<link rel="stylesheet" type="text/css" href="/Assets/css/styles.css">
		<script type="text/javascript" src="/Assets/js/jquery-3.2.1.min.js"></script>
		<script type="text/javascript" src="/Assets/js/bootstrap.min.js"></script>

		<meta charset="UTF-8">
		<title>
			{% block title %}
				ShoesRental -
			{% endblock %}
		</title>

			{% block custom_assets %}

			{% endblock %}
	</head>

	<body>
		<header>
			<div class="header">
				{% if not session.isConnected %}
					<a class="enregistrer" href="{{ path_for('register') }}">Sign in</a>

				{% else %}
   				 	<a class="enregistrer" href="{{ path_for('logOut') }}">Sign out</a>
   				 	<div class="welcome">
   				 		<span>Welcome</span> {{ session.isConnected.full_name }}
   				 	</div>
				{% endif %}
			</div>

			<div class="button">
				<ul>
					<li><a href="{{ path_for('index') }}">Home</a></li>
					<li><a href="{{ path_for('all_shops') }}">Shops</a></li>
					<li><a href="{{ path_for('shoes') }}">Shoes</a></li>

					{% if session.isConnected %}
						<li><a href="{{ path_for('display_bag') }}">Bag <div class='bagCount'>{% if bag_count > 0 %} {{bag_count}} {% endif %}</div></a></li>
						<li><a href="{{ path_for('orders_list') }}">Orders</a></li>
					{% endif %}
				</ul>
			</div>
		</header>

			{% block content %}

			{% endblock %}

		<footer>
			<div>

				<a class="lien-footer" href="{{ path_for('index') }}">Home -</a>
				<a class="lien-footer" href="{{ path_for('shoes') }}">All shoes -</a>
				<a class="lien-footer" href="{{ path_for('contact') }}">Contact us</a>

				<p>© 2017 ShoesRental</p>
			</div>
		</footer>
	</body>
</html>
