#!/usr/bin/env php
<?php

require __DIR__ .'/vendor/autoload.php';
require __DIR__ .'/config/database.php';

use Illuminate\Database\Capsule\Manager as Capsule;
use Cartalyst\Sentinel\Native\Facades\Sentinel;

class Migrator {
	
    /**
     * migrate the database schema
     */
    public function migrate() {
        /**
         * create table for sentinel user
         */
        if (!Capsule::schema()->hasTable('User')) {
            Capsule::schema()->create('User', function($table)
            {
                $table->increments('id_user');
                $table->string('full_name');
                $table->string('email');
                $table->string('password');
                $table->string('created_date');
                $table->string('last_latitude');
                $table->string('last_longitude');          

                // We'll need to ensure that MySQL uses the InnoDB engine to
                // support the indexes, other engines aren't affected.
                $table->engine = 'InnoDB';
                $table->unique('email');
            });
        }

        /**
         * create table for sentinel order
         */
        if (!Capsule::schema()->hasTable('Order')) {
            Capsule::schema()->create('Order', function($table)
            {
                $table->increments('id_order');
                $table->integer('id_user');
                $table->integer('id_order_line');
                $table->integer('total_price')->default(0);
                $table->timestamp('order_date');
                $table->timestamps();

                // We'll need to ensure that MySQL uses the InnoDB engine to
                // support the indexes, other engines aren't affected.
                $table->engine = 'InnoDB';
                $table->unique('id_user');
                $table->unique('id_order_line');

            });
        }

        /**
         * create order_line table
         */
        if (!Capsule::schema()->hasTable('Order_line')) {
            Capsule::schema()->create('Order_line', function($table)
            {
                $table->increments('id_order_line');
				$table->integer('id_shoes');
                $table->integer('quantity');
                $table->date('start_date');
                $table->date('end_date');
                $table->timestamps();

                // We'll need to ensure that MySQL uses the InnoDB engine to
                // support the indexes, other engines aren't affected.
                $table->engine = 'InnoDB';
				$table->unique('id_shoes');
            });
        }
		
		/**
         * create shoes table
         */
        if (!Capsule::schema()->hasTable('Shoes')) {
            Capsule::schema()->create('Shoes', function($table)
            {
				$table->increments('id_shoes');
                $table->integer('id_brand');
                $table->integer('id_user');
                $table->integer('id_shop');
                $table->integer('size');
                $table->integer('price_per_day');
                $table->boolean('is_reserved');
                $table->timestamps();

                // We'll need to ensure that MySQL uses the InnoDB engine to
                // support the indexes, other engines aren't affected.
                $table->engine = 'InnoDB';
                $table->unique('id_brand');
                $table->unique('id_user');
                $table->unique('id_shop');


            });
        }
		
		/**
         * create brand table
         */
        if (!Capsule::schema()->hasTable('Brand')) {
            Capsule::schema()->create('Brand', function($table)
            {
				$table->increments('id_brand');
                $table->string('legend');
                $table->timestamps();

                // We'll need to ensure that MySQL uses the InnoDB engine to
                // support the indexes, other engines aren't affected.
                $table->engine = 'InnoDB';
            });
        }
		
		/**
         * create shop table
         */
        if (!Capsule::schema()->hasTable('Shop')) {
            Capsule::schema()->create('Shop', function($table)
            {
                $table->increments('id_shop');
				$table->integer('id_city');
                $table->string('shop_name');
                $table->string('formated_adress');
                $table->string('latitude');
                $table->string('logitude');
                $table->timestamps();

                // We'll need to ensure that MySQL uses the InnoDB engine to
                // support the indexes, other engines aren't affected.
                $table->engine = 'InnoDB';
                $table->unique('id_city');
                
            });
        }

        /**
         * create city table
         */
        if (!Capsule::schema()->hasTable('City')) {
            Capsule::schema()->create('City', function($table)
            {
                $table->increments('id_city');
                $table->string('name');
                $table->timestamps();

                // We'll need to ensure that MySQL uses the InnoDB engine to
                // support the indexes, other engines aren't affected.
                $table->engine = 'InnoDB';
            });
        }
    }

    /**
     * seed the database with initial value
     */
    public function seed() {
        try {
           $credentials = [
				// 'email'    => 'admin@admin.com',
				// 'password' => 'password',
				// 'first_name' => 'Yudi',
				// 'last_name' => 'Purwanto',
			];

			$user = Sentinel::registerAndActivate($credentials);
			
        } catch(Exception $e) {
            echo $e->getMessage()."\n";
        }
    }
}

$migrator = new Migrator();

$migrator->migrate();
// $migrator->seed();

print("Migration Successfully");